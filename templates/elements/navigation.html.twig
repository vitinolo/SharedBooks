<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container">
      <a class="navbar-brand" href="{{ path('app_book') }}" title="retour à l'accueil" aria-label="back" style="font-size:25px">&ensp;Let's Go to...</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mx-auto" style="font-size:25px">
            <li class="nav-item me-5">
              <a class="nav-link" href="{{ path('app_book') }}" title="retour à l'accueil" aria-label="home">Home</a>
            </li>
          <li class="nav-item me-5">
            <a class="nav-link" href="{{ path('library') }}" title="vers les librairies" aria-label="librairies">Libraries</a>
          </li>
          <li class="nav-item me-5">
            <a class="nav-link" href="{{ path('gender') }}" title="vers les genres" aria-label="genre">Kind</a>
          </li>
          <li class="nav-item me-5">
            <a class="nav-link" href="{{ path('about') }}" title="vers à propos" aria-label="à propos">About</a>
          </li>
        </ul>
        {% if app.request.attributes.get('_route') == 'app_book' %}
        {% elseif app.request.attributes.get('_route') == 'library' %}
          <form class="d-flex" method="GET" action="{{ path('search_library') }}">
            <input class="form-control me-sm-2" type="search" placeholder="Find a library" name="library_name">
            <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
          </form>
        {% elseif app.request.attributes.get('_route') == 'gender' %}
          <form class="d-flex" method="GET" action="{{ path('search_books_by_kind') }}">
            <input class="form-control me-sm-2" type="search" placeholder="Find all books of kind" name="kind_name">
            <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
          </form>
        {% elseif app.request.attributes.get('_route') == 'about'%}
          
        {% endif %}
        
      </div>
    </div>
  </nav>